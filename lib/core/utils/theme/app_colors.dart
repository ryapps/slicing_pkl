import 'package:flutter/material.dart';

class AppColors {
  /// Primary brand color
  static const Color primary = Color(0xFF0D5EF4);
  static const Color primaryDark = Color(0xFF0A4AC0);

  /// Secondary / aksen jika diperlukan
  static const Color secondary = Color(0xFF0069AB);

  /// Neutral colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  static const Color greyLight = Color(0xFFF4F4F4);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color greyDark = Color(0xFF616161);

  /// Background global
  static const Color background = Color(0xFFF9F9F9);

  /// Error color
  static const Color error = Color(0xFFE53935);

  /// Success
  static const Color success = Color(0xFF43A047);

  /// Warning
  static const Color warning = Color(0xFFFFA000);

  /// Transparent
  static const Color transparent = Colors.transparent;
}
